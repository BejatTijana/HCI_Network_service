<UserControl x:Class="NetworkService.Views.NetworkDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#F5F5F5">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="Entities" FontSize="12" Foreground="#666666"
                   Margin="24,16,0,4"/>
        <Border Width="342" Height="120" BorderBrush="#CCCCCC" BorderThickness="1"
                Background="White" HorizontalAlignment="Left" Margin="24,0,0,0">
            <TreeView ItemsSource="{Binding EntityGroups}"
                      BorderThickness="0" Background="Transparent"
                      Cursor="Hand" ToolTip="Drag entity to place it on the grid"
                      PreviewMouseLeftButtonDown="TV_PreviewMouseDown"
                      MouseMove="TV_MouseMove">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Entities}">
                        <HierarchicalDataTemplate.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Naziv, StringFormat='&#x2022; {0}'}"
                                           Foreground="#666666" FontSize="12"/>
                            </DataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                        <TextBlock Text="{Binding TypeName, StringFormat='&#x25BC; {0}'}"
                                   Foreground="#444444" FontSize="12"/>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>
        <TextBlock Text="Drag &amp; Drop grid" FontSize="12" Foreground="#666666"
                   Margin="24,16,0,8"/>
        <Grid Margin="24,0,24,0">
        <ItemsControl x:Name="SlotsControl" ItemsSource="{Binding CanvasSlots}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="3" Columns="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Width="76" Height="70" Margin="2"
                          AllowDrop="True"
                          Drop="Slot_Drop"
                          DragOver="Slot_DragOver"
                          MouseDown="Slot_MouseDown"
                          ToolTip="{Binding DisplayText}">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOutOfRange}" Value="True">
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect BlurRadius="4" ShadowDepth="2"
                                                                  Opacity="0.25" Color="Black"/>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Rectangle RadiusX="10" RadiusY="10"
                                   Fill="White" Stroke="#CCCCCC" StrokeThickness="1">
                            <Rectangle.Style>
                                <Style TargetType="Rectangle">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsOutOfRange}" Value="True">
                                            <Setter Property="StrokeDashArray" Value="4,2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Rectangle.Style>
                        </Rectangle>
                        <TextBlock Text="{Binding DisplayText}"
                                   TextAlignment="Center" TextWrapping="Wrap"
                                   FontSize="9"
                                   HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#000000"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsOutOfRange}" Value="True">
                                            <Setter Property="Foreground" Value="#FF0000"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <Border BorderThickness="2" IsHitTestVisible="False">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelectedForConnection}" Value="True">
                                            <Setter Property="BorderBrush" Value="#3355FF"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Canvas x:Name="ConnectionCanvas" IsHitTestVisible="False"/>
        </Grid>
    </StackPanel>
</UserControl>
